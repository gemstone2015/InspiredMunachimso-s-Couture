require('dotenv').config();
const fs=require('fs');const path=require('path');const db=require('../db');
const tables=['admin_settings','products','product_media','customers','customer_measurements','customer_wishlist','customer_password_resets','preorders','preorder_files','order_status_history','messages','appointments','testimonials','notification_log','media_assets','media_trash','gallery_albums','gallery_items','collections','collection_products','campaigns'];
(async()=>{try{await db.ready;const data={createdAt:new Date().toISOString(),database:'postgresql',tables:{}};for(const table of tables){data.tables[table]=(await db.pool.query(`SELECT * FROM "${table}"`)).rows;}const dir=process.env.BACKUP_DIR||path.join(__dirname,'..','backups');fs.mkdirSync(dir,{recursive:true});const file=path.join(dir,`neon-backup-${new Date().toISOString().replace(/[:.]/g,'-')}.json`);fs.writeFileSync(file,JSON.stringify(data,null,2));console.log(`PostgreSQL JSON backup created: ${file}`);await db.close();}catch(e){console.error(e);process.exit(1)}})();
