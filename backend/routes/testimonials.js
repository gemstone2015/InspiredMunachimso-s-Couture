const express=require('express');const db=require('../db');const adminAuth=require('../middleware/adminAuth');const router=express.Router();
router.get('/',(_q,res)=>res.json(db.prepare(`SELECT * FROM testimonials WHERE is_published=1 ORDER BY sort_order,id DESC`).all()));
router.get('/admin/all',adminAuth,(_q,res)=>res.json(db.prepare(`SELECT * FROM testimonials ORDER BY sort_order,id DESC`).all()));
router.post('/',adminAuth,(req,res)=>{const {customer_name,location,quote,rating,image_url,is_published,sort_order}=req.body;if(!customer_name||!quote)return res.status(400).json({error:'Customer name and review are required.'});const r=db.prepare(`INSERT INTO testimonials (customer_name,location,quote,rating,image_url,is_published,sort_order) VALUES (?,?,?,?,?,?,?)`).run(customer_name,location||'',quote,Math.min(5,Math.max(1,Number(rating)||5)),image_url||'',is_published?1:0,Number(sort_order)||0);res.status(201).json(db.prepare('SELECT * FROM testimonials WHERE id=?').get(r.lastInsertRowid))});
router.put('/:id',adminAuth,(req,res)=>{const c=db.prepare('SELECT * FROM testimonials WHERE id=?').get(req.params.id);if(!c)return res.status(404).json({error:'Review not found.'});db.prepare(`UPDATE testimonials SET customer_name=?,location=?,quote=?,rating=?,image_url=?,is_published=?,sort_order=?,updated_at=datetime('now') WHERE id=?`).run(req.body.customer_name??c.customer_name,req.body.location??c.location,req.body.quote??c.quote,Number(req.body.rating??c.rating),req.body.image_url??c.image_url,req.body.is_published?1:0,Number(req.body.sort_order??c.sort_order),c.id);res.json(db.prepare('SELECT * FROM testimonials WHERE id=?').get(c.id))});
router.delete('/:id',adminAuth,(req,res)=>{db.prepare('DELETE FROM testimonials WHERE id=?').run(req.params.id);res.json({deleted:true})});module.exports=router;
